apiVersion: v1
kind: Service
metadata:
  labels:
    name: openshift-gitops-operator
  name: controller-manager-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    name: openshift-gitops-operator
