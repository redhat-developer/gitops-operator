apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
- script: |
    if [ -z $CI ]; then
      label_value=$(oc get  subscriptions openshift-gitops-operator -n openshift-operators -o json | jq '.spec.config.env[]|select(.name=="REMOVE_MANAGED_BY_LABEL_ON_ARGOCD_DELETION").value')
      if [ -z $label_value ]; then
        echo "REMOVE_MANAGED_BY_LABEL_ON_ARGOCD_DELETION not set"
        exit 1
      else
        exit 0
      fi
    else
      label_value=$(oc get subscription gitops-operator-v0-0-3-sub -n openshift-operators -o json | jq '.spec.config.env[]|select(.name=="REMOVE_MANAGED_BY_LABEL_ON_ARGOCD_DELETION").value')
      if [ -z $label_value ]; then
        echo "REMOVE_MANAGED_BY_LABEL_ON_ARGOCD_DELETION not set"
        exit 1
      else
        exit 0
      fi
    fi